---
name: tri-track-dev
description: 三轨稳定开发法。用于所有编程项目的开发管理:通过 main/staging/dev 三分支棘轮式沉淀正确代码,契约先行、模块化实现、测试定义完成。当用户要求开发功能、修复 bug、重构代码或开始新项目时,必须使用本技能。
---

# 三轨稳定开发法 (Tri-Track Development)

本技能约束 AI 编程行为,确保:正确的代码只进不退,错误的修改随时可回滚,项目目标永不丢失。

---

## 〇、会话启动检查(每次会话开始必做,不可跳过)

1. 读取 `GOALS.md`,向用户汇报:已完成 ✅ / 进行中 🔧 / 未开始 ⬜ 的数量,以及当前正在做哪个目标
2. 读取 `ARCHITECTURE.md` 和 `CONTRACTS.md`,恢复架构与接口认知
3. 执行 `git branch --show-current` 确认当前在 `dev` 分支;若不在,切换到 `dev`
4. 执行 `git status` 确认工作区干净;若有未提交的残留修改,先向用户汇报,等待指示

> 若上述文件不存在,说明项目未初始化,先执行「附录 A:项目初始化流程」。

---

## 一、三分支模型(铁律)

| 分支 | 角色 | 规则 |
|------|------|------|
| `main` | 稳定版(棘轮) | **禁止直接编写或修改任何代码**。只接受通过全部测试的 cherry-pick |
| `staging` | 兜底快照 | 上一个"确认能跑"的状态。只通过脚本同步,禁止手动修改 |
| `dev` | 试验田 | 所有开发工作只在这里进行。改崩了立即回滚到 staging |

### 绝对禁止的行为
- ❌ 在 `main` 上直接写代码、改代码、改配置
- ❌ 未跑测试就声称"已完成"或"已修复"
- ❌ 一次修改涉及多个目标(GOALS.md 中的多个条目)
- ❌ "顺手优化"当前目标之外的任何代码,哪怕只是改个变量名
- ❌ 发现契约(CONTRACTS.md)不合理时擅自改代码绕过——必须停下汇报
- ❌ 用 `--no-verify` 绕过 Git hook

---

## 二、开发三步流程

### 第一步:收敛(定规则)——新项目或新大功能时执行
1. 与用户确认技术栈、UI 风格、代码规范 → 写入 `ARCHITECTURE.md`
2. 将大功能拆解为模块(每模块 = 单一职责,约几百行以内,AI 一次会话可完整理解)
3. 为每个模块定义接口契约(输入/输出/错误处理/边界情况)→ 写入 `CONTRACTS.md`
4. 为每个模块建立目标条目(含验收标准)→ 写入 `GOALS.md`
5. **出口标准**:能对照文档回答"共几个模块、每个模块输入输出是什么"

### 第二步:发散(实现模块)——核心工作循环
```
1. 从 GOALS.md 选取一个(且仅一个)🔧 或 ⬜ 目标
2. 若该目标无对应测试,先按验收标准编写测试(测试先行)
3. 在 dev 上实现/修改,范围仅限该目标涉及的文件
4. 运行:该目标的测试 + 全量回归测试
   ├─ 全部通过 → 执行【代码自审查清单】(见第七节),确认无遗留问题
   │   └─ 审查通过 → 进入「三、沉淀流程」
   │   └─ 审查不通过 → 修复问题,重新运行测试+审查
   └─ 失败 → 立即执行 rollback（回滚到 staging，防止污染基础版本）
       ├─ 执行 git diff 对比失败版本与回滚版本的差异，定位修改了哪些内容
       ├─ 分析 Bug 根因：改了什么导致出问题？为什么会出错？
       ├─ 将本次 Bug 的根因和经验记录到【第七节·历史 Bug 经验沉淀】中
       └─ 向用户汇报：出了什么问题、根因分析、下一步修复方案
           └─ 等待用户确认后，在干净的 dev（回滚后的 staging）上重新尝试修复
               ├─ 再次失败 → 再次回滚 → 重复上述流程（每次失败都回滚，绝不在错误代码上继续改）
               └─ 成功 → 进入审查 + 沉淀流程
```

### 第三步:收敛(拼装集成)——相关模块全部 ✅ 后执行
1. 确认参与拼装的所有模块在 GOALS.md 中均为 ✅
2. **只编写胶水代码**(调用、组装、路由),禁止修改任何模块内部实现
3. 发现模块之间不匹配 → 停止编码,向用户汇报是哪两个模块、哪个接口不匹配,由用户决定修改哪份契约
4. 运行集成测试 + 端到端测试,通过后按「三、沉淀流程」处理

---

## 三、沉淀流程(dev → main,唯一合法路径)

目标测试全部通过后,依次执行:

```bash
# 1. 在 dev 上提交(一个目标 = 一个 commit,消息注明目标 ID)
git add <仅该目标涉及的文件>
git commit -m "feat(G-XX): <目标描述>"

# 2. 执行沉淀脚本(cherry-pick 到 main → main 上重跑全量测试 → 同步 staging)
bash scripts/promote.sh <commit-hash>
```

脚本成功后:
1. 更新 `GOALS.md`:该目标状态改为 ✅,记录 commit hash 和日期
2. 向用户汇报:本次沉淀了什么、GOALS.md 最新进度
3. 返回第二步,处理下一个目标

脚本失败(main 上测试不过)时:
1. **禁止在 main 上修复**,脚本会自动还原 main
2. 回到 dev 分析原因(通常是隐藏依赖:改动依赖了目标范围外的文件)
3. 补全 commit 内容或拆分目标后重试

---

## 四、回滚纪律

满足以下任一条件,立即执行 `bash scripts/rollback.sh`(dev 重置到 staging):

- **任何一次测试失败**（不再等到累计 3 次，详见第二步流程）
- dev 上出现无法解释的连环报错
- 发现自己已经修改了目标范围之外的文件且难以撤销
- 用户明确说"回滚"

### 回滚后必须执行

1. 确认当前在 dev 上,且 dev 已重置为 staging 的状态
2. **执行 `git log` 和 `git diff`，查看刚才的修改内容与失败版本的差异**
3. **分析 Bug 根因，记录到【第七节·历史 Bug 经验沉淀】**
4. **更新【第七节·代码自审查清单】——如果本次 Bug 揭示了缺失的审查项，立即补充**
5. 向用户汇报：改了什么、出了什么问题、根因分析、下一步方案
6. 等用户确认后，在干净的 dev 上重试

**回滚不是失败,是本方法论的正常机制。每次回滚都是一次经验积累。**

---

## 五、契约变更流程

实现中发现 CONTRACTS.md 设计有漏洞时:

1. 停止编码,向用户说明:哪个契约、什么漏洞、建议如何修改
2. 用户确认后,先改 `CONTRACTS.md`,再评估影响了哪些已 ✅ 的模块
3. 受影响模块状态改回 🔧,按第二步流程逐个更新并重新沉淀

**铁律:契约变更永远先发生在文档,文档是唯一真相源。**

---

## 六、汇报格式

每完成一轮循环(沉淀或回滚),用以下格式向用户汇报:

```
📋 [G-XX] <目标名称>
状态: ✅ 已沉淀 / 🔄 已回滚
测试: 单元 X/X 通过 | 回归 X/X 通过
进度: 已完成 X / 共 Y 个目标
下一步: <建议处理的下一个目标>
```

---

## 七、代码自审查清单(完成实现后、提交前必做)

每次完成代码修改后(无论新增功能还是修 bug),必须逐项执行以下检查。

### 通用检查

- [ ] **JS 模板字面量(反引号)完整性**
  - 检查总反引数是否为偶数: `grep -o "`" file | wc -l`
  - 重点检查含 `innerHTML=\`` 的行,确保闭合反引号存在
  - 常见出错:对模板字面量做字符串替换时误删了闭合反引号或中间模板代码

- [ ] **UTF-8 BOM 检查**
  - 检查文件头是否为 `EF BB BF`: `xxd -l 4 file | head -1`
  - 正确应为 `3C 21 44 4F`(`<!DOCTYPE`) 或 `3C 68 74 6D`(`<html`)
  - 保存文件时:用 Python 的 `encoding="utf-8"`(无 BOM),禁用 `encoding="utf-8-sig"`

- [ ] **JS 函数定义完整性**
  - 搜索所有 `onclick="xxx()"` / `onkeydown="xxx()"` 等引用
  - 确认每个被引用的函数名在文件中均有 `function xxx(...)` 定义
  - 常见出错:JS 解析到语法错误提前崩溃,导致后面所有函数"未定义"

- [ ] **括号/花括号/方括号配对**
  - 检查 `{` `}` 数量: `grep -o "{" file | wc -l` == `grep -o "}" file | wc -l`
  - 检查 `(` `)` `[` `]` 同理
  - 常见出错:minified JS 中括号嵌套层级深,容易漏掉闭合

- [ ] **文件编码一致性**
  - 文件统一 UTF-8 without BOM
  - 不要混用 GBK/GB2312/UTF-8 with BOM
  - 不要在 PowerShell 中用 `Set-Content -Encoding UTF8`(会加 BOM),改用 Python 写文件

- [ ] **版本号更新确认**
  - 标题/页面中的版本号是否已递增
  - 对比上一个版本的 tag/commit


- [ ] **字符串替换的引号完整性**
  - 用 `replace()` 做字符串替换时,新字符串的引号(单引号/双引号/反引号)必须与旧字符串完全对应
  - 常见出错:`"组 | 单价: ¥"+x.price` 替换为 `"组"` 时,如果直接调 `replace()`,JS的 `"组"+x.var` 结构中的引号配对会被破坏
  - 安全做法:用精确的索引切片替换,而非模糊的字符串匹配;替换后打印前后20字节检查引号配对
  - 示例:Python 中 `content.replace(old, new)` 如果 old/new 中包含引号,务必用 `repr()` 验证结果

- [ ] **JS 模板字面量边界检查**
  - 对含 `${...}` 的模板字面量做修改时,确保不破坏 `${` 和 `}` 的配对
  - 直接修改模板字面量内部文本比字符串拼接更安全(因为有明确的开闭定界符)
  - 修改后运行 `node --check file.js` 或 `python3 -c "js括号配对检查"` 验证

### 模板/前端专项检查

- [ ] **BAT 文件第一行无 BOM 污染**：检查 `.bat` 文件是否以 `EF BB BF` 开头
- [ ] **HTML 标签闭合**:检查 `<div>` `<table>` `<option>` 等标签是否完整配对
- [ ] **SVG 图标中无特殊字符**:SVG 字符串中不应包含未转义的 ` 或 ${}
- [ ] **模板字面量嵌套安全**:`${...}` 内部的字符串不应包含外层定界符
- [ ] **事件处理器参数正确**:`onclick="func(id)"` 中 id 是否是数字而非字符串(看有无引号包裹)

### 后端/API 专项检查

- [ ] **SQL 查询字段正确**:SELECT 的字段名与表结构一致
- [ ] **JSON 序列化兼容**:`json.dumps` 确保 `ensure_ascii=False` 以保留中文
- [ ] **API 返回格式统一**:统一返回 `{"success":true/false, "data":..., "message":"..."}`


- [ ] **BAT 文件 BOM 检查**
  - 用 `xxd -l 4 file.bat | head -1` 检查 BAT 文件头
  - 正确应为 `40 65 63 68`（即 `@ech` 的十六进制）
  - 如果开头为 `EF BB BF`，则包含 UTF-8 BOM，会导致 cmd 执行第一行乱码
  - 修复方法：用 Python 的 `open("file.bat", "w", encoding="utf-8")` 写入（无 BOM），禁用 `Out-File -Encoding utf8`（PowerShell 会加 BOM）
  - 常见症状：`锘緻echo`、`锘緾HS` 等乱码错误

### 历史 Bug 经验沉淀（回滚时强制记录，持续积累）

每次因修改出错触发回滚后，必须将本次 Bug 的根因记录到下方列表中。
记录格式：

```
## [YYYY-MM-DD] Bug: <简短描述>
- **涉及目标**: G-XX
- **修改了什么**: 改动了哪些文件/哪些行
- **出错现象**: 测试失败/运行时崩溃/逻辑错误
- **根因分析**: 为什么这个改动会触发 Bug
- **预防措施**: 以后改同类代码时，需重点审查什么
- **相关审查项**: 指向下文的某个审查复选框（如有）
```

**积累的意义**: 随着项目推进，逐步形成一份"容易踩坑的地方"清单。
后续开发时，每次提交前先浏览本清单中与当前修改相关的条目，
可大幅降低同类 Bug 重复出现的概率。

**铁律**:
- 每次回滚后必须记录，不可跳过
- 记录后更新【代码自审查清单】对应章节（如果发现缺失某项检查）
- 同一种 Bug 出现 2 次以上，必须在清单中加一条专用的审查项


### 快速审查脚本(可在项目根目录执行)

```bash
# 检查模板文件
python3 -c "
import sys
with open('templates/index.html', 'r') as f:
    c = f.read()
    # BOM 检查
    if c[0] == chr(0xfeff):
        print('[FAIL] 文件包含 UTF-8 BOM')
    else:
        print('[PASS] 无 BOM')
    # 反引号检查
    bt = c.count(chr(96))
    print(f'{"[PASS]" if bt % 2 == 0 else "[FAIL]"} 反引号: {bt}({"偶数" if bt % 2 == 0 else "奇数"})')
    # 花括号检查
    ob = c.count('{')
    cb = c.count('}')
    print(f'{"[PASS]" if ob == cb else "[FAIL]"} 花括号: open={ob} close={cb}')
    # 方括号检查
    osb = c.count('[')
    csb = c.count(']')
    print(f'{"[PASS]" if osb == csb else "[FAIL]"} 方括号: open={osb} close={csb}')
    # 关键函数检查
    import re
    onclick_funcs = set(re.findall(r'onclick="(\w+)\(', c))
    defined_funcs = set(re.findall(r'function (\w+)\(', c))
    missing = onclick_funcs - defined_funcs
    if missing:
        print(f'[FAIL] 以下函数被引用但未定义: {missing}')
    else:
        print('[PASS] 所有引用的函数均已定义')
"
```

### 审查通过标准

- 所有检查项标记 ✅ 通过
- 如发现任何 ❌ 失败项,必须先修复再提交
- 同一类问题连续出现 2 次以上,需将检查项加入本清单

---

## 附录 A:项目初始化流程

新项目首次使用本技能时:

```bash
git init
git checkout -b main
# 创建 ARCHITECTURE.md / CONTRACTS.md / GOALS.md(用模板,与用户逐项确认内容)
# 创建 scripts/promote.sh、scripts/rollback.sh、.git/hooks/pre-commit 并赋予执行权限
chmod +x scripts/*.sh .git/hooks/pre-commit
git add -A && git commit -m "chore: init tri-track project"
git branch staging
git checkout -b dev
```

初始化完成的标志:三分支存在、三份文档内容经用户确认、hook 可执行。

